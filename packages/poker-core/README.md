# Shared poker rules (engine + betting).
# Source of truth for NLHE evaluate/side pots.
# apps/poker-realtime and apps/web may copy or import from here.
# Prefer editing this package then syncing copies until workspace linking is wired.

