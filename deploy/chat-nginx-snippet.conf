# Production nginx — chat realtime (insert alongside poker-realtime / wander snippets)
# WebSocket upgrade for /ws/chat → apps/chat :3005
# REST history API under /api/chat → same upstream

location = /ws/chat {
    proxy_pass http://127.0.0.1:3005;
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "upgrade";
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_read_timeout 3600s;
    proxy_send_timeout 3600s;
}

location ^~ /api/chat/ {
    proxy_pass http://127.0.0.1:3005;
    proxy_http_version 1.1;
    client_max_body_size 30m;
    error_page 502 503 504 = @api_json_error;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
}
