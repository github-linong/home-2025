# Legacy excerpt from home-2023:sites-enabled/ws.lilnong.top.conf
# Sanitized for reference — NOT for production use.
# See deploy/legacy-nginx-notes.md


    server {
		server_name ws.lilnong.top;
		location / {
			proxy_set_header X-Real-IP $remote_addr;
			proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
			proxy_set_header Host $http_host;
			proxy_set_header X-NginX-Proxy true;
		 
			proxy_pass http://localhost:8090/;
			proxy_redirect off;
		 
			proxy_http_version 1.1;
			proxy_set_header Upgrade $http_upgrade;
			proxy_set_header Connection "upgrade";
		}
		location /suyue {
			proxy_set_header X-Real-IP $remote_addr;
			proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
			proxy_set_header Host $http_host;
			proxy_set_header X-NginX-Proxy true;
		 
			proxy_pass http://localhost:8091/;
			proxy_redirect off;
		 
			proxy_http_version 1.1;
			proxy_set_header Upgrade $http_upgrade;
			proxy_set_header Connection "upgrade";
		}
		location /sum {
			proxy_set_header X-Real-IP $remote_addr;
			proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
			proxy_set_header Host $http_host;
			proxy_set_header X-NginX-Proxy true;
		 
			proxy_pass http://localhost:8093/;
			proxy_redirect off;
		 
			proxy_http_version 1.1;
			proxy_set_header Upgrade $http_upgrade;
			proxy_set_header Connection "upgrade";
		}
	    listen 80; # managed by Certbot

	    # listen 443 ssl; # managed by Certbot
    # [redacted: certificate / letsencrypt path]
    # [redacted: certificate / letsencrypt path]
    # [redacted: certificate / letsencrypt path]
    # [redacted: certificate / letsencrypt path]

	}
