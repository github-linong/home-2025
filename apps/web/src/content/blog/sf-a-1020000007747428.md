---
title: "如何判断构造函数被实例化了几次"
heroImage: "/heroes/blog/sf-a-1020000007747428.webp"
description: "### 解答思路 本质上就是统计“这段构造代码被执行了几次”，最简单可靠的方式是给构造函数增加一个计数器，每次实例化时自增。 ### 示例代码 ```js function Person(name) { Person.count = (Person.count || 0) + 1; this.name = name; "
pubDate: "Dec 9, 2016"
badge: "思否问答"
tags: ["问答"]
source: "segmentfault"
sourceUrl: "https://segmentfault.com/q/1010000007747323/a-1020000007747428"
kind: "answer"
answerId: "1020000007747428"
votes: 5
accepted: true
questionUrl: "https://segmentfault.com/q/1010000007747323"
---

### 解答思路
本质上就是统计“这段构造代码被执行了几次”，最简单可靠的方式是给构造函数增加一个计数器，每次实例化时自增。

### 示例代码
```js
function Person(name) {
  Person.count = (Person.count || 0) + 1;
  this.name = name;
}

new Person("a");
new Person("b");
console.log(Person.count); // 2
```

### 详细说明
把计数器挂在构造函数自身的静态属性上（如 `Person.count`），每次 `new Person()` 执行时在构造函数体内自增。这样计数与实例的生命周期无关，即使实例被销毁，计数值依然准确保留了“历史实例化次数”。

### 小结
判断构造函数被实例化的次数，核心思路就是“计数”：在构造函数内部维护一个静态计数器，每次执行 `new` 时自增即可。
