---
title: "是否有现成的开源组件可以直接使用呢？"
description: "### 解答思路 这种上头部、下方三栏（左固定宽、中间自适应、右固定宽）的布局，用原生 Flex 布局就可以直接实现，不必依赖额外的开源组件。 ### 示例代码（如有代码） ```html <div class=\"box\"> <div class=\"header\"></div> <div class=\"content\""
pubDate: "Mar 29, 2023"
badge: "思否问答"
tags: ["CSS"]
source: "segmentfault"
sourceUrl: "https://segmentfault.com/q/1010000043595367/a-1020000043596307"
kind: "answer"
answerId: "1020000043596307"
votes: 2
accepted: true
questionUrl: "https://segmentfault.com/q/1010000043595367"
---

### 解答思路
这种上头部、下方三栏（左固定宽、中间自适应、右固定宽）的布局，用原生 Flex 布局就可以直接实现，不必依赖额外的开源组件。

### 示例代码（如有代码）
```html
<div class="box">
  <div class="header"></div>
  <div class="content">
    <div class="left"></div>
    <div class="panel"></div>
    <div class="right"></div>
  </div>
</div>
```

```css
.box {
  height: 100vh;
  display: flex;
  flex-direction: column;
}
.header {
  height: 40px;
}
.content {
  display: flex;
  overflow: hidden;
  flex: auto;
}
.left,
.right {
  flex: 0 0 200px;
}
.panel {
  flex: auto;
}
```

### 详细说明
- 外层 `.box` 用 `flex-direction: column` 将头部和内容区上下排列，头部固定高度 `40px`。
- `.content` 内部再用一层默认的水平 Flex 布局：`.left` 和 `.right` 用 `flex: 0 0 200px` 固定宽度不参与伸缩，中间 `.panel` 用 `flex: auto` 自动填充剩余空间。
- `.content` 设置 `overflow: hidden`，避免内部子元素内容溢出撑开整体布局。

### 小结
这类经典三栏布局无需引入第三方组件，用 Flex 的 `flex-direction: column` 嵌套 `flex: 0 0 固定宽度` / `flex: auto` 组合即可实现，代码简单且兼容性好。

