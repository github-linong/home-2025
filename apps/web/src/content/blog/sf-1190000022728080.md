---
title: "前端培训-中级阶段（40）- vue 2.x 路由 vue-router"
heroImage: "/heroes/blog/sf-1190000022728080.webp"
description: "前端最基础的就是 HTML+CSS+Javascript。掌握了这三门技术就算入门，但也仅仅是入门，现在前端开发的定义已经远远不止这些。前端小课堂（HTML/CSS/JS），本着提升技术水平，打牢基础知识的中心思想，我们开课啦（每周四）。"
pubDate: "Oct 2 2020"
tags: ["前端培训","Vue","JavaScript"]
badge: "思否"
source: "segmentfault"
sourceUrl: "https://segmentfault.com/a/1190000022728080"
---

前端最基础的就是 `HTML+CSS+Javascript`。掌握了这三门技术就算入门，但也仅仅是入门，现在前端开发的定义已经远远不止这些。前端小课堂（`HTML/CSS/JS`），本着提升技术水平，打牢基础知识的中心思想，我们开课啦（每周四）。

单页应用的话，我们主要还是要依赖 Vue-router 来实现路由系统。今天我们来看看 vue-router 日常使用的内容。

之前我们有一些系统是用 vue-element-admin 来做的，作者写的不错，路由权限、动态路由权限都有现成的方案。找实现方法可以直接看一下开源的代码。

## vue-router 支持的特性

Vue Router 是 Vue.js 官方的路由管理器。它和 Vue.js 的核心深度集成，让构建单页面应用变得易如反掌。包含的功能有：
- 嵌套的路由/视图表
`<router-view>` 嵌套，然后定义 `routes` 时定义 `children`
- 模块化的、基于组件的路由配置
- 
路由参数、查询、通配符
比如动态路由：定义的时候 `/user/:id`，获取的时候`this.$route.params.id`
- 比如通配符 `*`：一般是在 router 最后定义，指向一个 404 显示
- 可以获取查询字符串 `$route.query`，也可以获取锚点 `$route.hash`
- 基于 Vue.js 过渡系统的视图过渡效果
- 细粒度的导航控制
- 带有自动激活的 CSS class 的链接
- HTML5 历史模式或 hash 模式，在 IE9 中自动降级
- 自定义的滚动条行为

## vue-router 使用

### 可以运行的demo

```
<script src="https://unpkg.com/vue-router/dist/vue-router.js"></script>

<div id="app">
  <h1>Hello App!</h1>
  <p>
    <!-- 使用 router-link 组件来导航. -->
    <!-- 通过传入 `to` 属性指定链接. -->
    <!-- <router-link> 默认会被渲染成一个 `<a>` 标签 -->
    <router-link to="/foo">Go to Foo</router-link>
    <router-link to="/bar">Go to Bar</router-link>
  </p>
  <!-- 路由出口 -->
  <!-- 路由匹配到的组件将渲染在这里 -->
  <router-view></router-view>
</div>
<script>
// 0. 如果使用模块化机制编程，导入Vue和VueRouter，要调用 Vue.use(VueRouter)

// 1. 定义 (路由) 组件。
// 可以从其他文件 import 进来
const Foo = { template: '<div>foo</div>' }
const Bar = { template: '<div>bar</div>' }

// 2. 定义路由
// 每个路由应该映射一个组件。 其中"component" 可以是
// 通过 Vue.extend() 创建的组件构造器，
// 或者，只是一个组件配置对象。
// 我们晚点再讨论嵌套路由。
const routes = [
  { path: '/foo', component: Foo },
  { path: '/bar', component: Bar }
]

// 3. 创建 router 实例，然后传 `routes` 配置
// 你还可以传别的配置参数, 不过先这么简单着吧。
const router = new VueRouter({
  routes // (缩写) 相当于 routes: routes
})

// 4. 创建和挂载根实例。
// 记得要通过 router 配置参数注入路由，
// 从而让整个应用都有路由功能
const app = new Vue({
  router
}).$mount('#app')

// 现在，应用已经启动了！
</script>
```

### 导航守卫、路由钩子函数

提供了跳转或取消导航的能力。分为全局、路由、组件级。
参数或查询的改变并不会触发进入/离开的导航守卫，可以通过观察 `$route` 对象来应对这些变化，或使用 `beforeRouteUpdate` 的组件内守卫。

#### 全局级别的钩子函数

全局级别是指定义在 `router` 上，比如 `router.beforeEach`。
- 
`beforeEach` 全局前置钩子函数。当一个导航触发时，全局前置守卫按照创建顺序调用。守卫是异步解析执行，此时导航在所有守卫 resolve 完之前一直处于 等待中。

```
const router = new VueRouter({ ... })
router.beforeEach((to, from, next) => {
    // to: Route: 即将要进入的目标路由对象
    // from: Route: 当前导航正要离开的路由
    // next()：执行下一个钩子，必须调用，否则会一直等待
    // next(false)：中断此次跳转
    // next(path)：跳转到新的地址，一般是权限判断，无权限去登陆页
    // next(error): (2.4.0+)触发 onError 事件
})  
```

- `beforeResolve` 全局解析钩子函数 ( 2.5.0+)。这和 `router.beforeEach` 类似，区别是在导航被确认之前，同时在所有组件内守卫和异步路由组件被解析之后，解析守卫就被调用。
- `afterEach` 全局后置钩子函数

#### 路由级别的钩子函数

路由级别是指在 `routes` 定义时设置的。

```
const router = new VueRouter({
  routes: [
    {
      path: '/foo',
      component: Foo,
      beforeEnter: (to, from, next) => {
        // ...
      }
    }
  ]
})
```

#### 组件级别的钩子函数

组件级别就是指在定义组件时写的了，和 `mounted` 同级。比如路由改变，但是因为组件共享问题，需要刷新这个时候我们可以在 `beforeRouteUpdate` 里面做逻辑，或者 `watch` 监听 `$route`
- `beforeRouteEnter` 渲染组件前被调用，无法获取this
- `beforeRouteUpdate` (2.2 新增) 在当前路由改变，但是该组件被复用时调用
- `beforeRouteLeave` 导航离开该组件的对应路由时调用。可以用来实现禁止用户在还未保存修改时异常离开，提示保存内容。

#### 完整的导航解析流程
- 导航被触发。
- 在失活的组件里调用 `beforeRouteLeave` 守卫。
- 调用全局的 `beforeEach` 守卫。
- 在重用的组件里调用 `beforeRouteUpdate` 守卫 (2.2+)。
- 在路由配置里调用 `beforeEnter`。
- 解析异步路由组件。
- 在被激活的组件里调用 `beforeRouteEnter`。
- 调用全局的 `beforeResolve` 守卫 (2.5+)。
- 导航被确认。
- 调用全局的 `afterEach` 钩子。
- 触发 DOM 更新。
- 调用 `beforeRouteEnter` 守卫中传给 `next` 的回调函数，创建好的组件实例会作为回调函数的参数传入。

### 路由对象属性 $route

下面的属性介绍，统一都基于下面的配置：
页面URL: `https://www.lilnong.top/cors/vueRouter?a=1#b`
路由规则：`/cors/:key`
- path 对应当前路由的路径，总是解析为绝对路径 `/cors/vueRouter`
- params 对应路由规则中的参数。`this.$route.params.key == 'vueRouter'`
- query 对应的是查询字符串。`this.$route.query.a == '1'`
- hash 对应的就是锚点。`this.$route.hash == '#b'`
- fullPath 对应的就是完整的URL。
- matched 包含的是所有匹配的路由规则。
- name 包含的是当前路由的name属性
- redirectedFrom 对应的是重定向的来源位置。

### 高级技巧
- 
组件与路由系统解耦，组件开发中我们一般是定义 `props`，但是路由系统却是 `$route` 这种冲突破坏了我们组件，降低了复用性。这个时候我们在定义 `routes` 时，给组件设置 `props: true`，这样 `route.params` 将会被设置为组件属性。
`props: true`：将 `route.params` 设置为组件属性
- `props: { aaa: false }`：直接将这个对象，传进去为组件属性
- `props: function`：通过编程的方式生成一个对象，传进去当组件属性
- 
路由携带入参（路由元信息 meta），有时候我们需要给路由设置一些信息，比如是否缓存，可以访问的角色&权限控制。

定义时

```
const router = new VueRouter({
  routes: [
    {
      path: '/config',
      component: config,
      meta: {role:['admin']}
    }
  ]
})
```

- 获取时 `$route.meta.role`。也可以在钩子函数中获取 `to.matched.map(record => record.meta.role)`, matched 可以获取到所有被匹配到的路由记录
- 路由懒加载，我们需要结合 Vue 的异步组件和 Webpack 的代码分割功能，来实现路由组件的懒加载。
Vue 中允许组件是异步的`const Foo = () => Promise.resolve({ /* 组件定义对象 */ }) `
配合 webpack 引入资源就变成了 `const Foo = () => import(/* webpackChunkName: "group-foo" */ './Foo.vue') `，`/* webpackChunkName: "group-foo" */` 是一个按组分块打包的功能，可以看个人是否使用了。
- 动态添加的路由规则，`router.addRoutes(routes: Array<RouteConfig>) `

## 常见的注意项
- 使用路由参数时，组件会复用，复用的组件生命周期钩子不会再次被调用。可以通过 `watch` 监听 `$route` 来监听改变，也可以通过 `beforeRouteUpdate` 钩子函数（vueRouter@2.2 版本支持）。
- `*` 匹配 404 时，搭配着动态路由使用时，出现无法响应后加的路由问题。这是因为匹配规则是先匹配前面定义的，如果找到就不会继续匹配了（匹配的优先级就按照路由的定义顺序：谁先定义的，谁的优先级就最高。）。所以解决方案就是把404匹配规则也做到动态添加里面。

## 微信公众号：前端linong

## 参考文献
- 前端培训目录、前端培训规划、前端培训计划

---

> 原文：[SegmentFault](https://segmentfault.com/a/1190000022728080)
