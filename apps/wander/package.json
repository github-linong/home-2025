{
  "name": "@personal-site/wander",
  "version": "0.1.0",
  "private": true,
  "description": "Real-time multiplayer 2D wander game over WebSocket — server-authoritative rooms on a grid map",
  "type": "module",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "test": "node --test tests/*.test.js"
  },
  "dependencies": {
    "ws": "^8.18.0"
  }
}
