{
  "name": "@personal-site/api2",
  "version": "0.1.0",
  "private": true,
  "description": "Better Auth API for lilnong.top (ESM)",
  "type": "module",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "dev": "node --watch --env-file=../../.env --env-file=.env src/server.js",
    "start": "node --env-file=../../.env --env-file=.env src/server.js",
    "auth:migrate": "node --env-file=../../.env --env-file=.env src/migrate.js",
    "test": "node --test tests/*.test.js"
  },
  "dependencies": {
    "better-auth": "^1.3.7",
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "pg": "^8.16.3"
  }
}
