{
  "name": "@personal-site/chat",
  "version": "0.1.0",
  "private": true,
  "description": "Real-time chat WebSocket service: public group, named groups (game rooms), and DMs, with login + anonymous guest support",
  "type": "module",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "test": "node --test tests/*.test.js"
  },
  "dependencies": {
    "mysql2": "^3.23.2",
    "ws": "^8.18.0"
  }
}
